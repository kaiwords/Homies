{
  "name": "homies-admin-api",
  "description": "Small authenticated backend for admin-only operations that require the Firebase Admin SDK (e.g. fully deleting a user's Auth login), which the Flutter client can never do directly.",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.6.0"
  }
}

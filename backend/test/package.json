{
  "name": "leasely-rules-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Firestore security-rules unit tests (run against the emulator).",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0"
  }
}

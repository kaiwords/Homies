# File Storage (Firebase Storage)

Firebase Storage holds binary uploads referenced by the app as `Attachment` records: bill proofs, ID documents, bond/advance-rent proofs, cleaning task photos, grocery receipts, and issue photos.

| File | Purpose |
|------|---------|
| `storage.rules` | Security rules for file read/write. **Starter template — harden before production.** |
